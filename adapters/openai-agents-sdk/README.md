# Moved

Importable package is now `adapters.openai_agents` (hyphens are invalid in Python module names).

```python
from adapters.openai_agents import make_trilith_tools
```

See [docs/adapters.md](../../docs/adapters.md).
